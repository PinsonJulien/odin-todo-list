import Icon, { FontVariationSettings } from "./icon";

export default class HorizontalRule extends Icon {
  constructor (
    fontVariationSettings?: FontVariationSettings
  ) {
    super('horizontal_rule', fontVariationSettings);
  }
}